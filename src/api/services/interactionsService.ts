// import {commentOnEvent, fetchComments} from '@/src/api/endpoints/interactions';
//
// const interactionsService = {
//   getComments: async (eventId: string | undefined) => {
//     return await fetchComments(eventId);
//   },
//   comment: async (comment: string, eventId: string) => {
//     return await commentOnEvent(comment, eventId);
//   },
// };
//
// export default interactionsService;
